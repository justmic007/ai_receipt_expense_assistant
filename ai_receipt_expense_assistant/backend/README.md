# ai_receipt_expense_assistant

```
AI Receipt & Expense Assistant is your entry point. It's the most approachable to build but immediately gets you hands-on with the Anthropic API, structured output, file processing, and a real deployed full-stack app. You'll design prompts that extract structured data (merchant, amount, category) from messy image/PDF inputs — this directly maps to the "data processing for AI features" and "prompt design" requirements.
```
